/**
 * This code squares any number in you
 * put as an argument
 * but i wrote a default number "x=2" 
 * in the parameter
 * 
 * @param {number} -x is a number
 * @returns {string}
 */
 let square = function(x=2) {
    return x*x;
    }
    console.log(`This is a squared number ${square()}`);
    